
pipeline {
   agent any
   stages {
      stage('git stage') {
         steps {
             //  g
             srript {
                properties([
                        parameters([
                            choice(
                                choices: ['ONE', 'TWO'], 
                                name: 'PARAMETER_01'
                            )

             }
         
         }
      }
   }
}
